# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BitCoin Tracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BitCoin Tracker

  target 'BitCoin TrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BitCoin TrackerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

pod 'Alamofire', '~> 4.5'
pod 'SwiftyJSON'
pod 'Charts'
pod 'SwiftChart'
end
